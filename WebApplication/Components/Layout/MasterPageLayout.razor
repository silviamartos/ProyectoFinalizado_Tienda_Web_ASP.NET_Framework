
@inherits LayoutComponentBase
@inject NavigationManager NavigationManager
@using Dominio

<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Origin</title>
    <link rel="icon" type="image/x-icon" href="favicon.ico" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        html, body {
            height: 100%;
            margin: 0;
        }

        .content {
            flex: 1;
        }

        footer {
            padding: 10px;
        }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <div class="d-flex flex-column flex-grow-1">
        <nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark" style="height: 80px; display: flex; align-items: center; justify-content: center">
            <div style="margin: 15px">
                <a style="margin-left: 10px" class="navbar-brand" href="#">New Origin</a>
                <br />
                @if (usuarioActivo != null && usuarioActivo.TipoDeUsuario == 1)
                {
                    <a style="font-size: 15px;" href="/Default">Modo administrador</a>
                }
            </div>
            <a href="/PerfilDeUsuario">
                <img style="height: 40px; border-radius: 45%; width: 40px" src="@iconoImagenSrc" alt="..." />
            </a>
        </nav>
        <hr />
        <div class="container content">
            @Body
        </div>
        <hr />
        <footer>Argentina - Buenos Aires</footer>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>

@code {
    private Usuarios? usuarioActivo;
    private string iconoImagenSrc = "https://img.freepik.com/vector-premium/icono-marco-fotos-foto-vacia-blanco-vector-sobre-fondo-transparente-aislado-eps-10_399089-1290.jpg";

    protected override void OnInitialized()
    {
        try
        {
            // Note: Session management in Blazor Server requires a different approach
            // You'll need to implement a state management service or use ProtectedSessionStorage
            // For now, this is a placeholder structure
            
            // Example: Inject a session service to get the active user
            // usuarioActivo = SessionService.GetUsuarioActivo();
            
            // If you have a session service, uncomment and adapt the following:
            /*
            if (usuarioActivo != null)
            {
                if (!string.IsNullOrEmpty(usuarioActivo.ImagenDePerfil))
                {
                    iconoImagenSrc = usuarioActivo.ImagenDePerfil;
                }
                else
                {
                    iconoImagenSrc = "https://img.freepik.com/vector-premium/icono-marco-fotos-foto-vacia-blanco-vector-sobre-fondo-transparente-aislado-eps-10_399089-1290.jpg";
                }
            }
            */
        }
        catch (Exception ex)
        {
            // Log the exception or handle it appropriately
            // In Blazor, you might want to inject ILogger<MasterPageLayout>
            throw;
        }
    }
}